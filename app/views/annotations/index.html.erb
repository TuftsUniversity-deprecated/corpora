<h1>Annotation locations</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Json</th>
    <th>Text</th>
    <th>Pid</th>
  </tr>

<% @annotations.each do |annotation| %>
  <tr>
    <td><%= location.name %></td>
    <td><%= location.description %></td>
    <td><%= location.link %></td>
    <td><%= location.modern_location %></td>
    <td><%= link_to 'Show', annotation %></td>
    <td><%= link_to 'Edit', edit_annotation_path(annotation) %></td>
    <td><%= link_to 'Destroy', annotation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Location', new_location_path %>
